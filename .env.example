# Google Calendar MCP Server Configuration
# Transport mode
TRANSPORT=stdio  # Recommended for Claude Desktop integration

# HTTP settings (when TRANSPORT=http)
# WARNING: HTTP mode is for local development only - no authentication layer
PORT=3000
HOST=127.0.0.1  # Use 127.0.0.1 for security; 0.0.0.0 exposes to network (not recommended)

# Optional: HTTP path for MCP endpoint (defaults to /mcp)
# GOOGLE_CALENDAR_MCP_PATH=/mcp
GOOGLE_CALENDAR_MCP_PATH=

# Optional: DNS rebinding protection for the MCP endpoint (/mcp).
# This is useful when running behind a gateway or in cloud runtimes.
# - GOOGLE_CALENDAR_MCP_ENABLE_DNS_REBINDING_PROTECTION=true
# - GOOGLE_CALENDAR_MCP_ALLOWED_HOSTS=google-calendar-mcp:3000,calendar.example.com
# - GOOGLE_CALENDAR_MCP_ALLOWED_ORIGINS=https://gateway.example.com
GOOGLE_CALENDAR_MCP_ENABLE_DNS_REBINDING_PROTECTION=
GOOGLE_CALENDAR_MCP_ALLOWED_HOSTS=
GOOGLE_CALENDAR_MCP_ALLOWED_ORIGINS=

# Optional: allow specific non-localhost Origins for the account-management HTTP API only.
# This affects requests to `/api/accounts*` (NOT `/mcp`).
# Comma-separated exact origins (scheme + host + optional port), e.g.:
# GOOGLE_CALENDAR_MCP_ALLOWED_ORIGINS_FOR_ACCOUNTS=https://gateway.example.com,https://admin.example.com
GOOGLE_CALENDAR_MCP_ALLOWED_ORIGINS_FOR_ACCOUNTS=

# Optional: public base URL used to generate OAuth redirect URIs in HTTP mode.
# Without this, `/api/accounts` will generate redirect URIs using `http://${HOST}:${PORT}` (which breaks in cloud deployments like `0.0.0.0`).
#
# Example:
# GOOGLE_CALENDAR_MCP_PUBLIC_BASE_URL=https://calendar-mcp.example.com
GOOGLE_CALENDAR_MCP_PUBLIC_BASE_URL=

# OAuth credentials path (required)
GOOGLE_OAUTH_CREDENTIALS=./gcp-oauth.keys.json

# Cloud OAuth callback for the interactive auth flow (optional)
# When set, the `auth` / `manage-accounts` flow will use this public URL as the OAuth redirect URI
# instead of hardcoded `http://localhost:<port>/oauth2callback`.
#
# Example:
# GOOGLE_CALENDAR_MCP_AUTH_CALLBACK_URL=https://calendar.example.com/oauth2callback
# GOOGLE_CALENDAR_MCP_AUTH_SERVER_PORT=3500
GOOGLE_CALENDAR_MCP_AUTH_CALLBACK_URL=
GOOGLE_CALENDAR_MCP_AUTH_SERVER_PORT=

## Optional: Custom token storage location
# GOOGLE_CALENDAR_MCP_TOKEN_PATH=/custom/path/to/tokens

## OPTIONAL: Test Configuration (for development/testing only)
# TEST_CALENDAR_ID=your-test-calendar@gmail.com

## OPTIONAL: Multi-Account Testing (requires 2 authenticated Google accounts)
# Authenticate accounts with:
#   node scripts/account-manager.js auth test-primary
#   node scripts/account-manager.js auth test-secondary
#
# TEST_PRIMARY_ACCOUNT=test-primary
# TEST_PRIMARY_CALENDAR=your-primary-test-email@gmail.com
# TEST_SECONDARY_ACCOUNT=test-secondary
# TEST_SECONDARY_CALENDAR=your-secondary-test-email@gmail.com
# TEST_SHARED_CALENDAR=calendar-id-shared-between-accounts@group.calendar.google.com

## OPTIONAL: Anthropic API config (used only for integration testing)
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# CLAUDE_API_KEY={your_api_key}

# OPTIONAL: Open AI API config (used only for integration testing)
# OPENAI_API_KEY={your_api_key}
# OPENAI_MODEL=gpt-4.1
